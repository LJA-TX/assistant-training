# Repo Layout

- `configs/lora/`: versioned LoRA/QLoRA training configs
- `manifests/runs/`: run manifests (inputs, outputs, launch template, gates)
- `scripts/`: preflight and training entrypoints
- `tests/`: dataset/schema/contract tests
- `artifacts/`: local outputs (adapters, logs)
- `docs/`: process and migration documentation
