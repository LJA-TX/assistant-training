# Compatibility Pointer

This directory remains on its historical path for compatibility and discoverability.

Canonical process-infrastructure assets now live under `docs/framework/process_infrastructure/`.

Use the exact-file aliases in this subtree only as preserved compatibility paths, not as canonical authority.
