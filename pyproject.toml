[project]
name = "assistant-training"
version = "0.1.0"
description = "Training workflows for assistant-runtime model fine-tuning"
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
